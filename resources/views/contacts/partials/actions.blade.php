<button type="button" class="btn btn-sm btn-primary edit-contact" data-id="{{ $contact->id }}" title="Edit">
    <i class="fas fa-edit"></i> Edit
</button>
<button type="button" class="btn btn-sm btn-danger delete-contact" data-id="{{ $contact->id }}" data-name="{{ $contact->name }}" title="Delete">
    <i class="fas fa-trash"></i> Delete
</button>
